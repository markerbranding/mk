<ul role="list" class="nav__ul nav__ul__left">
    {% for item in menuIzq %}
        <li>
            <a href="{{ item.url }}" class="{% if page.url == item.url %}active{% endif %}" >
                {{ item.name }}
            </a>
        </li>
    {% endfor %}
</ul>